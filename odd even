#include<iostream>
using namespace std;
int main()
{
  int number;
  cout<<"enter an integer:";
  cin>>number;
  if(number%2==0)
  {
   cout<<number<<"is even."<<std::ends;
  }
  else
  {
   cout<<number<<"is odd."<<std::ends;
  }
  return 0;
}
